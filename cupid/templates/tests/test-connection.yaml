apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cupid.fullname" . }}-test-connection"
  labels:
    {{- include "cupid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cupid.fullname" . }}:{{ .Values.venus.service.port }}']
  restartPolicy: Never
