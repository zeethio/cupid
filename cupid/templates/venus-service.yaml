apiVersion: v1
kind: Service
metadata:
  name: {{ include "cupid.fullname" . }}-venus-service
  labels:
    app: {{ .Values.venus.selectorLabel }}
spec:
  type: {{ .Values.venus.service.type }}
  ports:
    - port: {{ .Values.venus.service.port }}
      nodePort:  {{ .Values.venus.service.nodePort }}
      protocol: TCP
  selector:
    app: {{ .Values.venus.selectorLabel }}
